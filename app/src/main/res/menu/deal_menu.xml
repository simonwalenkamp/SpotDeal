<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
        <item
                android:id="@+id/load_active"
                android:icon="@drawable/ic_valid"
                android:title="@string/active"/>
        <item
            android:id="@+id/load_inactive"
            android:icon="@drawable/ic_invalid"
            android:title="@string/inactive"/>

        <item
                android:id="@+id/load_all"
                android:icon="@drawable/ic_all"
                android:title="@string/all"/>
</menu>